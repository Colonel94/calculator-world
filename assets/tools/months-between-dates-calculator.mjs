import {assert,payment,future,date,iso,days,gcd,payoff} from '../core.mjs';
export default {...{"id":"months-between-dates-calculator","title":"Months Between Dates Calculator","category":"dates","description":"Count complete calendar months using the starting day as the monthly anniversary.","fields":[{"key":"start","label":"Start date","value":"2026-01-15","type":"date"},{"key":"end","label":"End date","value":"2026-04-14","type":"date"}],"formula":"Months = (end year − start year) × 12 + end month − start month; subtract one if the end day is earlier than the start day.","notes":"End must not precede start. The same day-of-month is required for a complete month; January 31 to February 28 is zero complete months under this convention.","example":"January 15 to April 14 is two complete months; through April 15 it is three.","faq":["Is every month treated as 30 days?","No. This compares calendar fields and does not convert days using an average month length."]},solve: x=>{const a=date(x.start),b=date(x.end);assert(b>=a,'End date must be on or after start date.');return {'Complete calendar months':(b.getUTCFullYear()-a.getUTCFullYear())*12+b.getUTCMonth()-a.getUTCMonth()-(b.getUTCDate()<a.getUTCDate()?1:0)};}};
